# 프로덕션 빌드(npm run build) 전용 환경변수.
# GitHub Actions 배포는 .env(gitignore 대상)를 읽을 수 없으므로 이 파일이 필요하다.
# VITE_ 변수는 어차피 번들에 그대로 노출되므로 커밋해도 비밀이 새지 않는다.
VITE_API_BASE_URL=https://api.aice-edu.site
VITE_PYTHON_AI_URL=http://3.35.179.26:5000
